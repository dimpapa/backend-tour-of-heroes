'use strict'

const { ImageModel } = require('./image.model');
const { LabelModel } = require('./label.model');
const { ProductModel } = require('./product.model');
const { ViewProductsLabelsModel } = require('./view-products-labels.model');

module.exports = { ImageModel, LabelModel, ProductModel, ViewProductsLabelsModel };
